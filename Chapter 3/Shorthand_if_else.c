#include <stdio.h>

int main() {
    //condition ? expression-if-true : expression-if-false
    // Here "?" and ":" are called Ternary Operators
    int a = 12,b = 25;
    a < b ? printf("A is less then B"): printf("B is less than A");
    
    
    return 0;
}